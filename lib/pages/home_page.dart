import 'package:flutter/material.dart';

import 'placeholder_page.dart';

/// "What is Breast Cancer?" home page.
/// Replace with the real cards (What is Breast Cancer, Why Early Detection
/// Matters, How the App Helps You, Translate to Telugu button, etc).
class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(title: 'What is Breast Cancer?');
  }
}